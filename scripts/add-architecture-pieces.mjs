#!/usr/bin/env node
/**
 * Adds 27 architectural pieces to public/brickforge.html — domes, chhatris, jali
 * screens, cusped arches, battlements, glass curtain walls, viaducts, road
 * junctions and street furniture — and gives five existing placeholder pieces
 * real geometry.
 *
 * It patches the file in place rather than replacing it, so anything else in
 * your builder (worlds, tools, demo cities) is left untouched.
 *
 *   node scripts/add-architecture-pieces.mjs [path/to/brickforge.html]
 *
 * Running it twice is safe: the second run detects the marker and exits.
 */
import { readFileSync, writeFileSync, copyFileSync, existsSync } from "node:fs";

const target = process.argv[2] ?? "public/brickforge.html";
if (!existsSync(target)) {
  console.error(`Cannot find ${target}. Pass the path to brickforge.html as an argument.`);
  process.exit(1);
}

let html = readFileSync(target, "utf8");
const MARKER = "machine === 'chhatri'";
if (html.includes(MARKER)) {
  console.log("Already patched — nothing to do.");
  process.exit(0);
}

const HELPERS = "/* --- profiles and lathe helpers for domes, spires, columns and basins --- */\nconst DOME_PROFILE = (function () {\n  const pts = [];\n  for (let i = 0; i <= 14; i++) {\n    const a = (i / 14) * Math.PI / 2;\n    pts.push([Math.cos(a), Math.sin(a)]);\n  }\n  return pts;\n})();\nconst ONION_PROFILE = [\n  [0.55, 0], [0.74, 0.05], [0.9, 0.12], [1.0, 0.24], [0.98, 0.36], [0.88, 0.48],\n  [0.73, 0.6], [0.55, 0.71], [0.37, 0.81], [0.21, 0.9], [0.09, 0.96], [0.0, 1]\n];\nconst FINIAL_PROFILE = [\n  [0.5, 0], [0.34, 0.1], [0.52, 0.2], [0.3, 0.32], [0.18, 0.44],\n  [0.3, 0.54], [0.14, 0.64], [0.08, 0.8], [0.03, 0.92], [0.0, 1]\n];\n/** Revolve a [radiusFraction, heightFraction] profile into a solid. */\nfunction latheOf(profile, radius, height, segments, baseY) {\n  const points = profile.map(([x, y]) => new THREE.Vector2(Math.max(0.0015, x * radius), y * height));\n  const g = new THREE.LatheGeometry(points, segments || 20);\n  if (baseY) g.translate(0, baseY, 0);\n  return g;\n}\n/** A ring of blocks \u2014 a basin wall or a dome collar, with no normal surprises. */\nfunction ringOf(radius, thickness, baseY, height, segments) {\n  const list = [];\n  const n = segments || 20;\n  for (let i = 0; i < n; i++) {\n    const a = (i / n) * Math.PI * 2;\n    const seg = new THREE.BoxGeometry(thickness, height, (Math.PI * 2 * radius) / n + 0.06);\n    seg.rotateY(-a);\n    seg.translate(Math.cos(a) * radius, baseY + height / 2, Math.sin(a) * radius);\n    list.push(seg);\n  }\n  return mergeGeoms(list);\n}\n/** A tapered column shaft with a base and capital. */\nfunction columnAt(radius, height, baseY, x, z) {\n  const list = [];\n  const shaft = new THREE.CylinderGeometry(radius * 0.86, radius, height - 0.3, 12);\n  shaft.translate(0, baseY + 0.15 + (height - 0.3) / 2, 0);\n  list.push(shaft);\n  const base = new THREE.BoxGeometry(radius * 2.5, 0.18, radius * 2.5);\n  base.translate(0, baseY + 0.09, 0);\n  list.push(base);\n  const cap = new THREE.BoxGeometry(radius * 2.7, 0.2, radius * 2.7);\n  cap.translate(0, baseY + height - 0.1, 0);\n  list.push(cap);\n  const merged = mergeGeoms(list);\n  merged.translate(x, 0, z);\n  return merged;\n}\n\n";
const BRANCHES = "  } else if (t.machine === 'dome' || t.machine === 'onion') {\n    const r = w / 2 - 0.05;\n    const top = Math.min(h * (t.machine === 'onion' ? 0.86 : 1), r * (t.machine === 'onion' ? 1.5 : 1.06));\n    parts.push(latheOf(t.machine === 'onion' ? ONION_PROFILE : DOME_PROFILE, r, top, w > 3 ? 30 : 20));\n    parts.push(ringOf(r + 0.06, 0.16, 0, 0.14, w > 3 ? 30 : 20));\n    if (t.machine === 'onion') {\n      parts.push(latheOf(FINIAL_PROFILE, r * 0.3, h - top, 14, top));\n    }\n  } else if (t.machine === 'finial') {\n    parts.push(boxAt(w - 0.2, 0.2, d - 0.2, 0, 0.1, 0));\n    parts.push(latheOf(FINIAL_PROFILE, w * 0.42, h - 0.2, 16, 0.2));\n  } else if (t.machine === 'chhatri') {\n    const r = w / 2 - 0.05, deck = BRICK_H * 0.5, colH = h * 0.42;\n    parts.push(boxAt(w - 0.02, deck, d - 0.02, 0, deck / 2, 0));\n    const inset = w / 2 - 0.7;\n    [[-inset, -inset], [inset, -inset], [-inset, inset], [inset, inset]].forEach(([x, z]) => {\n      parts.push(columnAt(0.3, colH, deck, x, z));\n    });\n    parts.push(boxAt(w - 0.3, 0.28, d - 0.3, 0, deck + colH + 0.14, 0));\n    const capR = r * 0.82, capY = deck + colH + 0.28;\n    const capH = Math.min(h - capY - 0.5, capR * 1.05);\n    parts.push(latheOf(DOME_PROFILE, capR, capH, 22, capY));\n    parts.push(latheOf(FINIAL_PROFILE, 0.22, h - capY - capH, 12, capY + capH));\n  } else if (t.machine === 'jali') {\n    const th = w - 0.06, bars = Math.max(3, Math.round(d * 1.5)), rows = 4;\n    parts.push(boxAt(th, 0.34, d - 0.02, 0, 0.17, 0));\n    parts.push(boxAt(th, 0.34, d - 0.02, 0, h - 0.17, 0));\n    parts.push(boxAt(th, h, 0.34, 0, h / 2, -d / 2 + 0.17));\n    parts.push(boxAt(th, h, 0.34, 0, h / 2, d / 2 - 0.17));\n    const inner = h - 0.68;\n    for (let i = 1; i < bars; i++) {\n      parts.push(boxAt(th * 0.62, inner, 0.14, 0, h / 2, -d / 2 + (i / bars) * d));\n    }\n    for (let j = 1; j < rows; j++) {\n      parts.push(boxAt(th * 0.62, 0.14, d - 0.68, 0, 0.34 + (j / rows) * inner, 0));\n    }\n  } else if (t.machine === 'steps') {\n    const n = 4, rise = h / n, run = d / n;\n    for (let i = 0; i < n; i++) {\n      parts.push(boxAt(w - 0.02, rise * (n - i), run + 0.01, 0, rise * (n - i) / 2, -d / 2 + run * (i + 0.5)));\n    }\n  } else if (t.machine === 'battlement') {\n    parts.push(boxAt(w - 0.02, h * 0.45, d - 0.02, 0, h * 0.225, 0));\n    const merlons = Math.max(2, Math.round(d / 2));\n    for (let i = 0; i < merlons; i++) {\n      const z = -d / 2 + (i + 0.5) * (d / merlons);\n      parts.push(boxAt(w - 0.02, h * 0.55, d / merlons - 0.5, 0, h * 0.45 + h * 0.275, z));\n    }\n  } else if (t.machine === 'cusp' || t.machine === 'grandarch') {\n    const th = w - 0.02, pierH = h * 0.3, span = d - 1.4;\n    parts.push(boxAt(th, pierH, 0.7, 0, pierH / 2, -d / 2 + 0.35));\n    parts.push(boxAt(th, pierH, 0.7, 0, pierH / 2, d / 2 - 0.35));\n    parts.push(boxAt(th, h - pierH - h * 0.55, d - 0.02, 0, h - (h - pierH - h * 0.55) / 2, 0));\n    const lobes = 5, steps = 26;\n    for (let i = 0; i < steps; i++) {\n      const f = (i + 0.5) / steps;\n      const base = Math.sin(Math.PI * f);\n      const cusp = 0.8 + 0.2 * Math.cos(f * Math.PI * lobes * 2);\n      const height = pierH + (h * 0.55) * base * cusp;\n      const z = -span / 2 + f * span;\n      parts.push(boxAt(th, h - height, span / steps + 0.02, 0, height + (h - height) / 2, z));\n    }\n  } else if (t.machine === 'jharokha') {\n    const deck = 0.34;\n    parts.push(boxAt(w - 0.02, deck, d - 0.02, 0, deck / 2, 0));\n    parts.push(boxAt(w - 0.02, h * 0.5, 0.28, 0, deck + h * 0.25, -d / 2 + 0.14));\n    [[-1, 0], [1, 0]].forEach(([sx]) => {\n      parts.push(boxAt(0.28, h * 0.5, d - 0.02, sx * (w / 2 - 0.14), deck + h * 0.25, 0));\n    });\n    for (let i = 0; i < 4; i++) {\n      parts.push(boxAt(0.12, h * 0.32, 0.12, -w / 2 + 0.3 + i * ((w - 0.6) / 3), deck + h * 0.16, d / 2 - 0.15));\n    }\n    parts.push(boxAt(w + 0.3, 0.22, d + 0.3, 0, deck + h * 0.5 + 0.11, 0));\n    parts.push(latheOf(DOME_PROFILE, w / 2 - 0.1, h - deck - h * 0.5 - 0.22, 18, deck + h * 0.5 + 0.22));\n  } else if (t.machine === 'column') {\n    const shaft = h - 0.9, r = w / 2 - 0.12;\n    parts.push(boxAt(w - 0.02, 0.34, d - 0.02, 0, 0.17, 0));\n    parts.push(latheOf([[1, 0], [0.92, 0.3], [0.86, 0.7], [0.9, 0.95], [0.86, 1]], r, shaft, 16, 0.34));\n    const flutes = 8;\n    for (let i = 0; i < flutes; i++) {\n      const a = (i / flutes) * Math.PI * 2;\n      const f = boxAt(0.1, shaft * 0.82, 0.1, Math.cos(a) * r * 0.94, 0.34 + shaft * 0.45, Math.sin(a) * r * 0.94);\n      parts.push(f);\n    }\n    parts.push(boxAt(w + 0.16, 0.3, d + 0.16, 0, h - 0.42, 0));\n    parts.push(boxAt(w - 0.1, 0.26, d - 0.1, 0, h - 0.13, 0));\n  } else if (t.machine === 'fortwall') {\n    const batter = 0.18;\n    const b = gbuilder(), V = (x, y, z) => new THREE.Vector3(x, y, z);\n    const wo = w / 2, wi = w / 2 - batter, z0 = -d / 2, z1 = d / 2, top = h * 0.78;\n    b.quad(V(-wo, 0, z0), V(-wi, top, z0), V(wi, top, z0), V(wo, 0, z0));\n    b.quad(V(-wo, 0, z1), V(wo, 0, z1), V(wi, top, z1), V(-wi, top, z1));\n    b.quad(V(-wo, 0, z0), V(-wo, 0, z1), V(-wi, top, z1), V(-wi, top, z0));\n    b.quad(V(wo, 0, z0), V(wi, top, z0), V(wi, top, z1), V(wo, 0, z1));\n    b.quad(V(-wo, 0, z0), V(wo, 0, z0), V(wo, 0, z1), V(-wo, 0, z1));\n    b.quad(V(-wi, top, z0), V(-wi, top, z1), V(wi, top, z1), V(wi, top, z0));\n    parts.push(b.build(new THREE.Vector3(0, top / 2, 0)));\n    parts.push(boxAt(w + 0.16, 0.24, d - 0.02, 0, top + 0.12, 0));\n    const merlons = Math.max(2, Math.round(d / 2));\n    for (let i = 0; i < merlons; i++) {\n      parts.push(boxAt(w - 0.1, h - top - 0.24, d / merlons - 0.45, 0,\n        top + 0.24 + (h - top - 0.24) / 2, -d / 2 + (i + 0.5) * (d / merlons)));\n    }\n  } else if (t.machine === 'tieredroof') {\n    const tiers = 3;\n    for (let i = 0; i < tiers; i++) {\n      const f = i / tiers, nf = (i + 1) / tiers;\n      const wi = w * (1 - f * 0.55), di = d * (1 - f * 0.55);\n      const y = h * f;\n      parts.push(boxAt(wi + 0.5, 0.22, di + 0.5, 0, y + 0.11, 0));\n      parts.push(boxAt(wi * (1 - 0.18), h / tiers - 0.22, di * (1 - 0.18), 0, y + 0.22 + (h / tiers - 0.22) / 2, 0));\n      if (i === tiers - 1) {\n        parts.push(latheOf(DOME_PROFILE, wi * 0.34, h * 0.4, 20, y + h / tiers));\n      }\n    }\n  } else if (t.machine === 'glasswall') {\n    parts.push(boxAt(w - 0.02, h, d - 0.02, 0, h / 2, 0));\n    const bays = Math.max(2, Math.round(d));\n    for (let i = 1; i < bays; i++) {\n      parts.push(boxAt(w + 0.1, h, 0.16, 0, h / 2, -d / 2 + i * (d / bays)));\n    }\n    for (let j = 1; j < 3; j++) {\n      parts.push(boxAt(w + 0.1, 0.16, d - 0.02, 0, (j / 3) * h, 0));\n    }\n    parts.push(boxAt(w + 0.1, 0.2, d - 0.02, 0, 0.1, 0));\n    parts.push(boxAt(w + 0.1, 0.2, d - 0.02, 0, h - 0.1, 0));\n  } else if (t.machine === 'bridge') {\n    parts.push(boxAt(w - 0.02, h * 0.45, d - 0.02, 0, h * 0.225, 0));\n    [-1, 1].forEach((s) => {\n      parts.push(boxAt(0.3, h, d - 0.02, s * (w / 2 - 0.15), h / 2, 0));\n      for (let i = 0; i < d; i++) {\n        parts.push(boxAt(0.14, h * 0.5, 0.14, s * (w / 2 - 0.15), h * 0.7, -d / 2 + 0.5 + i));\n      }\n    });\n  } else if (t.machine === 'pier') {\n    parts.push(boxAt(w + 0.4, 0.4, d + 0.4, 0, 0.2, 0));\n    parts.push(boxAt(w - 0.1, h - 0.8, d - 0.1, 0, h / 2, 0));\n    parts.push(boxAt(w + 0.5, 0.4, d + 0.5, 0, h - 0.2, 0));\n  } else if (t.machine === 'roadx' || t.machine === 'roadz') {\n    parts.push(boxAt(w - 0.02, h * 0.75, d - 0.02, 0, h * 0.375, 0));\n    if (t.machine === 'roadz') {\n      [-1, 1].forEach((s) => parts.push(boxAt(0.16, h, d - 0.02, s * (w / 2 - 0.2), h / 2, 0)));\n      for (let i = 0; i < 6; i++) {\n        parts.push(boxAt(w - 1.1, h * 0.9, 0.34, 0, h * 0.45, -d / 2 + 0.5 + i * ((d - 1) / 5)));\n      }\n    } else {\n      [[-1, 0], [1, 0], [0, -1], [0, 1]].forEach(([sx, sz]) => {\n        parts.push(boxAt(sx ? 1.1 : 0.34, h * 0.85, sz ? 1.1 : 0.34,\n          sx * (w / 2 - 0.55), h * 0.42, sz * (d / 2 - 0.55)));\n      });\n    }\n  } else if (t.machine === 'kerb') {\n    parts.push(boxAt(w - 0.02, h, d - 0.02, 0, h / 2, 0));\n    parts.push(boxAt(w * 0.5, h + 0.16, d - 0.02, -w * 0.24, (h + 0.16) / 2, 0));\n  } else if (t.machine === 'platform') {\n    parts.push(boxAt(w - 0.02, h * 0.82, d - 0.02, 0, h * 0.41, 0));\n    parts.push(boxAt(w - 0.02, h * 0.18, d - 0.02, 0, h * 0.91, 0));\n    parts.push(boxAt(0.34, h + 0.1, d - 0.02, -w / 2 + 0.17, (h + 0.1) / 2, 0));\n    for (let i = 0; i < d / 2; i++) {\n      parts.push(boxAt(0.22, 0.3, 0.22, w / 2 - 0.4, h + 0.15, -d / 2 + 1 + i * 2));\n    }\n  } else if (t.machine === 'fountain') {\n    const r = w / 2 - 0.1;\n    parts.push(ringOf(r, 0.34, 0, h * 0.42, 24));\n    const floor = new THREE.CylinderGeometry(r - 0.3, r - 0.3, 0.16, 24);\n    floor.translate(0, 0.08, 0); parts.push(floor);\n    parts.push(latheOf([[1, 0], [0.7, 0.25], [0.4, 0.5], [0.55, 0.62], [0.2, 0.8], [0.1, 1]], 0.7, h * 0.7, 16, h * 0.3));\n    const jet = new THREE.SphereGeometry(0.26, 10, 8);\n    jet.translate(0, h, 0); parts.push(jet);\n  } else if (t.machine === 'bench') {\n    parts.push(boxAt(w - 0.1, 0.18, d - 0.1, 0, h * 0.45, 0));\n    parts.push(boxAt(0.18, h * 0.5, d - 0.1, -w / 2 + 0.16, h * 0.72, 0));\n    [-1, 1].forEach((s) => {\n      parts.push(boxAt(w - 0.2, h * 0.45, 0.18, 0, h * 0.22, s * (d / 2 - 0.2)));\n    });\n  } else if (t.machine === 'hedge') {\n    parts.push(boxAt(w - 0.1, h * 0.8, d - 0.1, 0, h * 0.4, 0));\n    for (let i = 0; i < d * 2; i++) {\n      const s = new THREE.SphereGeometry(0.34, 8, 6);\n      s.translate((i % 2 ? 0.18 : -0.18), h * 0.8, -d / 2 + 0.25 + i * 0.5);\n      parts.push(s);\n    }\n  } else if (t.machine === 'flowers') {\n    parts.push(boxAt(w - 0.02, h * 0.5, d - 0.02, 0, h * 0.25, 0));\n    for (let i = 0; i < w * d * 2; i++) {\n      const a = i * 2.399, rr = 0.36 * Math.sqrt(i / (w * d * 2)) * w;\n      const s = new THREE.SphereGeometry(0.17, 7, 6);\n      s.translate(Math.cos(a) * rr, h * 0.5 + 0.14, Math.sin(a) * rr);\n      parts.push(s);\n    }\n  } else if (t.machine === 'parasol') {\n    parts.push(boxAt(0.18, h * 0.72, 0.18, 0, h * 0.36, 0));\n    const canopy = new THREE.ConeGeometry(w * 1.5, h * 0.3, 12);\n    canopy.translate(0, h * 0.85, 0); parts.push(canopy);\n    parts.push(boxAt(0.5, 0.16, 0.5, 0, 0.08, 0));\n  } else if (t.machine === 'signal') {\n    parts.push(boxAt(0.5, 0.16, 0.5, 0, 0.08, 0));\n    parts.push(boxAt(0.2, h * 0.7, 0.2, 0, h * 0.35, 0));\n    parts.push(boxAt(0.44, h * 0.3, 0.34, 0, h * 0.82, 0));\n    for (let i = 0; i < 3; i++) {\n      const lens = new THREE.CylinderGeometry(0.11, 0.11, 0.1, 10);\n      lens.rotateX(Math.PI / 2);\n      lens.translate(0, h * 0.72 + i * 0.2, 0.2);\n      parts.push(lens);\n    }\n  } else if (t.machine === 'chimney') {\n    parts.push(boxAt(w - 0.12, h * 0.8, d - 0.12, 0, h * 0.4, 0));\n    parts.push(boxAt(w + 0.16, 0.2, d + 0.16, 0, h * 0.8 + 0.1, 0));\n    const pot = new THREE.CylinderGeometry(0.2, 0.24, h * 0.16, 12);\n    pot.translate(0, h * 0.9, 0); parts.push(pot);\n  } else if (t.machine === 'ridge') {\n    const b = gbuilder(), V = (x, y, z) => new THREE.Vector3(x, y, z);\n    const x0 = -w / 2, x1 = w / 2, z0 = -d / 2, z1 = d / 2;\n    b.quad(V(x0, 0, z0), V(0, h, z0), V(0, h, z1), V(x0, 0, z1));\n    b.quad(V(x1, 0, z0), V(x1, 0, z1), V(0, h, z1), V(0, h, z0));\n    b.tri(V(x0, 0, z0), V(x1, 0, z0), V(0, h, z0));\n    b.tri(V(x0, 0, z1), V(0, h, z1), V(x1, 0, z1));\n    b.quad(V(x0, 0, z0), V(x0, 0, z1), V(x1, 0, z1), V(x1, 0, z0));\n    parts.push(b.build(new THREE.Vector3(0, h / 2, 0)));\n  } else if (t.machine === 'awning') {\n    [-1, 1].forEach((s) => {\n      parts.push(boxAt(0.16, h * 0.75, 0.16, w / 2 - 0.1, h * 0.375, s * (d / 2 - 0.3)));\n    });\n    const b = gbuilder(), V = (x, y, z) => new THREE.Vector3(x, y, z);\n    const back = -w / 2, front = w / 2 + 0.6, hi = h, lo = h * 0.62, z0 = -d / 2, z1 = d / 2;\n    b.quad(V(back, hi, z0), V(back, hi, z1), V(front, lo, z1), V(front, lo, z0));\n    b.quad(V(back, hi - 0.16, z0), V(front, lo - 0.16, z0), V(front, lo - 0.16, z1), V(back, hi - 0.16, z1));\n    b.quad(V(front, lo, z0), V(front, lo, z1), V(front, lo - 0.16, z1), V(front, lo - 0.16, z0));\n    b.quad(V(back, hi, z0), V(front, lo, z0), V(front, lo - 0.16, z0), V(back, hi - 0.16, z0));\n    b.quad(V(back, hi, z1), V(back, hi - 0.16, z1), V(front, lo - 0.16, z1), V(front, lo, z1));\n    parts.push(b.build(new THREE.Vector3((back + front) / 2, hi - 0.5, 0)));\n    const scallops = Math.max(3, Math.round(d * 1.5));\n    for (let i = 0; i < scallops; i++) {\n      const s = new THREE.SphereGeometry(0.22, 8, 6);\n      s.translate(front - 0.1, lo - 0.2, -d / 2 + (i + 0.5) * (d / scallops));\n      parts.push(s);\n    }\n";
const TYPES = "/* --- palace and heritage detail --- */\nT({ id:'onion4', label:'onion dome 4\u00d74', w:4, d:4, h:BRICK_H * 5, machine:'onion', tile:true, cat:'Heritage architecture' });\nT({ id:'onion6', label:'onion dome 6\u00d76', w:6, d:6, h:BRICK_H * 7, machine:'onion', tile:true, cat:'Heritage architecture' });\nT({ id:'dome6', label:'dome 6\u00d76', w:6, d:6, h:BRICK_H * 2.7, machine:'dome', tile:true, cat:'Heritage architecture' });\nT({ id:'finial1', label:'finial spire', w:1, d:1, h:BRICK_H * 3, machine:'finial', tile:true, cat:'Heritage architecture' });\nT({ id:'chhatri4', label:'chhatri 4\u00d74', w:4, d:4, h:BRICK_H * 6, machine:'chhatri', tile:true, cat:'Heritage architecture' });\nT({ id:'chhatri6', label:'chhatri 6\u00d76', w:6, d:6, h:BRICK_H * 8, machine:'chhatri', tile:true, cat:'Heritage architecture' });\nT({ id:'jali4', label:'jali screen 1\u00d74', w:1, d:4, h:BRICK_H * 3, machine:'jali', cat:'Heritage architecture' });\nT({ id:'jali8', label:'jali screen 1\u00d78', w:1, d:8, h:BRICK_H * 3, machine:'jali', cat:'Heritage architecture' });\nT({ id:'cusp4', label:'cusped arch 1\u00d74', w:1, d:4, h:BRICK_H * 4, machine:'cusp', cat:'Heritage architecture' });\nT({ id:'steps4', label:'steps 4\u00d74', w:4, d:4, h:BRICK_H * 2, machine:'steps', cat:'Heritage architecture' });\nT({ id:'steps8', label:'steps 8\u00d74', w:8, d:4, h:BRICK_H * 2, machine:'steps', cat:'Heritage architecture' });\nT({ id:'battle8', label:'battlement 1\u00d78', w:1, d:8, h:BRICK_H, machine:'battlement', tile:true, cat:'Heritage architecture' });\nT({ id:'jharokha2', label:'balcony 2\u00d72', w:2, d:2, h:BRICK_H * 3, machine:'jharokha', tile:true, cat:'Heritage architecture' });\n\n/* --- city structure --- */\nT({ id:'glass4', label:'glass wall 1\u00d74', w:1, d:4, h:BRICK_H * 3, machine:'glasswall', fixedColor:'#7fb6de', fixedAlpha:0.62, cat:'City modules' });\nT({ id:'glass8', label:'glass wall 1\u00d78', w:1, d:8, h:BRICK_H * 3, machine:'glasswall', fixedColor:'#7fb6de', fixedAlpha:0.62, cat:'City modules' });\nT({ id:'bridge4', label:'bridge deck 4\u00d74', w:4, d:4, h:BRICK_H, machine:'bridge', cat:'City modules' });\nT({ id:'pier2', label:'bridge pier 2\u00d72', w:2, d:2, h:BRICK_H * 4, machine:'pier', cat:'City modules' });\nT({ id:'platform8', label:'station platform 2\u00d78', w:2, d:8, h:BRICK_H, machine:'platform', cat:'City modules' });\nT({ id:'kerb4', label:'kerb 1\u00d74', w:1, d:4, h:PLATE_H * 1.6, machine:'kerb', fixedColor:'#c9ccd2', cat:'Roads & rails' });\nT({ id:'roadx', label:'crossroads 4\u00d74', w:4, d:4, h:PLATE_H, machine:'roadx', tile:true, town:{ road:true }, fixedColor:'#4a5058', cat:'Roads & rails' });\nT({ id:'roadz', label:'zebra crossing 4\u00d74', w:4, d:4, h:PLATE_H, machine:'roadz', tile:true, town:{ road:true }, fixedColor:'#4a5058', cat:'Roads & rails' });\n\n/* --- street furniture and planting --- */\nT({ id:'fountain4', label:'fountain 4\u00d74', w:4, d:4, h:BRICK_H * 2, machine:'fountain', tile:true, cat:'Street & garden' });\nT({ id:'bench2', label:'bench 1\u00d72', w:1, d:2, h:BRICK_H, machine:'bench', tile:true, cat:'Street & garden' });\nT({ id:'hedge4', label:'hedge 1\u00d74', w:1, d:4, h:BRICK_H, machine:'hedge', tile:true, fixedColor:'#3f8a3a', cat:'Street & garden' });\nT({ id:'flowers2', label:'flower bed 2\u00d72', w:2, d:2, h:PLATE_H * 2, machine:'flowers', tile:true, cat:'Street & garden' });\nT({ id:'parasol1', label:'parasol', w:1, d:1, h:BRICK_H * 3, machine:'parasol', tile:true, cat:'Street & garden' });\nT({ id:'signal1', label:'traffic light', w:1, d:1, h:BRICK_H * 3, machine:'signal', tile:true, cat:'Street & garden' });\nT({ id:'awning4', label:'market awning 1\u00d74', w:1, d:4, h:BRICK_H * 2, machine:'awning', tile:true, cat:'Street & garden' });\nT({ id:'chimney1', label:'chimney 1\u00d71', w:1, d:1, h:BRICK_H * 2, machine:'chimney', tile:true, cat:'Street & garden' });\nT({ id:'ridge8', label:'roof ridge 1\u00d78', w:1, d:8, h:PLATE_H * 2, machine:'ridge', tile:true, cat:'Street & garden' });\n\n";
const SWAPS = [
  [
    "T({ id:'heritageDome4', label:'dome 4×4', w:4, d:4, h:BRICK_H * 2, cone:true, cat:'Heritage architecture' });",
    "T({ id:'heritageDome4', label:'dome 4×4', w:4, d:4, h:BRICK_H * 1.8, machine:'dome', tile:true, cat:'Heritage architecture' });"
  ],
  [
    "T({ id:'heritageColumn1', label:'carved column 1×1', w:1, d:1, h:BRICK_H * 4, round:true, cat:'Heritage architecture' });",
    "T({ id:'heritageColumn1', label:'carved column 1×1', w:1, d:1, h:BRICK_H * 4, machine:'column', cat:'Heritage architecture' });"
  ],
  [
    "T({ id:'heritageColumn2', label:'grand column 2×2', w:2, d:2, h:BRICK_H * 5, round:true, cat:'Heritage architecture' });",
    "T({ id:'heritageColumn2', label:'grand column 2×2', w:2, d:2, h:BRICK_H * 5, machine:'column', cat:'Heritage architecture' });"
  ],
  [
    "T({ id:'heritageArch8', label:'monumental arch 1×8', w:1, d:8, h:BRICK_H * 4, arch:true, cat:'Heritage architecture' });",
    "T({ id:'heritageArch8', label:'monumental arch 1×8', w:1, d:8, h:BRICK_H * 4, machine:'grandarch', cat:'Heritage architecture' });"
  ],
  [
    "T({ id:'heritageWall8', label:'fort wall 1×8', w:1, d:8, h:BRICK_H * 2, cat:'Heritage architecture' });",
    "T({ id:'heritageWall8', label:'fort wall 1×8', w:1, d:8, h:BRICK_H * 2, machine:'fortwall', tile:true, cat:'Heritage architecture' });"
  ],
  [
    "T({ id:'heritageRoof8', label:'tiered roof 8×8', w:8, d:8, h:BRICK_H, slope:true, cat:'Heritage architecture' });",
    "T({ id:'heritageRoof8', label:'tiered roof 8×8', w:8, d:8, h:BRICK_H * 3, machine:'tieredroof', tile:true, cat:'Heritage architecture' });"
  ]
];
const OLD_COLORS = "['#c91a09','Trans red', 0.45], ['#5a93db','Trans blue', 0.45]";
const NEW_COLORS = "['#c91a09','Trans red', 0.45], ['#5a93db','Trans blue', 0.45],\n  ['#d8b171','Sandstone'], ['#b2532e','Terracotta'], ['#f3e6c8','Cream'],\n  ['#2fa89a','Jade'], ['#e08aa0','Rose'], ['#4c5560','Slate']";

const anchors = [
  ["function boxAt(w, h, d, x, y, z) {", "geometry helper anchor"],
  ["  } else if (t.wedge) {", "geometry branch anchor"],
  ["T({ id:'heritageRoad',", "catalogue anchor"],
];
for (const [needle, what] of anchors) {
  if (!html.includes(needle)) {
    console.error(`This does not look like a BrickLab builder file: missing the ${what}.`);
    process.exit(1);
  }
}

const before = html.length;
const insertBefore = (text, needle, block) => {
  const at = text.indexOf(needle);
  return text.slice(0, at) + block + text.slice(at);
};

html = insertBefore(html, "function boxAt(w, h, d, x, y, z) {", HELPERS);
html = insertBefore(html, "  } else if (t.wedge) {", BRANCHES);
html = insertBefore(html, "T({ id:'heritageRoad',", TYPES);

let upgraded = 0;
const skipped = [];
for (const [oldLine, newLine] of SWAPS) {
  if (html.includes(oldLine)) {
    html = html.replace(oldLine, newLine);
    upgraded++;
  } else {
    skipped.push(oldLine.slice(0, 40) + "...");
  }
}

let colours = 0;
if (html.includes(OLD_COLORS) && !html.includes("'Sandstone'")) {
  html = html.replace(OLD_COLORS, NEW_COLORS);
  colours = 6;
}

copyFileSync(target, target + ".backup");
writeFileSync(target, html, "utf8");

console.log(`Patched ${target}`);
console.log(`  backup written to ${target}.backup`);
console.log(`  27 pieces added, ${upgraded}/${SWAPS.length} placeholder pieces upgraded, ${colours} colours added`);
console.log(`  file grew by ${Math.round((html.length - before) / 1024)} KB`);
if (skipped.length) {
  console.log("  note: these pieces were not found, so their geometry was left alone:");
  for (const line of skipped) console.log("    " + line);
}
